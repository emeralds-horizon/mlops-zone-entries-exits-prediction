apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: zone-entries-prediction-staging-ingressroute
  namespace: istio-system
spec:
  entryPoints:
  - websecure
  routes:
  - kind: Rule
    match: Host(`zone-entries-prediction.stagingv2.kubeflow.emeralds.ari-aidata.eu`)
    services:
    - name: istio-ingressgateway
      port: 80
  tls:
    certResolver: letsencrypt
